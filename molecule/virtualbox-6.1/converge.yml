---
- name: Converge Virtualbox 6.1
  hosts: all
  become: true
  become_method: sudo
  vars:
    virtualbox_version: 6.1
  tasks:
  - name: "Include andrelohmann.virtualbox"
    include_role:
      name: "andrelohmann.virtualbox"
...
